import AddMedicalRecordsComponent from "./Pruebavacu"; // Cambia el nombre del import

const AddMedicalRecords = () => {
  return (
    <div>
      <AddMedicalRecordsComponent /> {/* Usa el nombre cambiado */}
    </div>
  );
};

export default AddMedicalRecords;

